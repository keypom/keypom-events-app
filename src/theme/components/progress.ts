import { defineStyleConfig } from "@chakra-ui/react";

export const ProgressStyle = defineStyleConfig({
  baseStyle: {
    track: {
      bg: "brand.800",
    },
    filledTrack: {
      bg: "brand.400",
    },
  },
});
